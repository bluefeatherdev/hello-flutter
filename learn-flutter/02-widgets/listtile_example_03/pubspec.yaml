name: listtile_example_03
description: "A Flutter demo app showcasing different ListTile configurations, including leading, trailing, dense, two-line, and three-line variations inside Card widgets."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
