name: iconbutton_example_05
description: "A Flutter demo app showcasing IconButton with toggleable states (isSelected, selectedIcon) across standard, filled, filled tonal, and outlined styles, including static previews with isSelected set to null."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
