name: listtile_example_07
description: "A Flutter demo app showcasing ListTile widgets wrapped in Expanded inside a Row to evenly distribute horizontal space."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
