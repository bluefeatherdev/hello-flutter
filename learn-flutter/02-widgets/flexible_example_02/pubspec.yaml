name: flexible_example_02
description: "A Flutter demo app showcasing the Flexible widget inside a Row to distribute horizontal space proportionally using flex factors."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
