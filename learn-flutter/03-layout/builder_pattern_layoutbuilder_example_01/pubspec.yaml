name: builder_pattern_layoutbuilder_example_01
description: "A Flutter demo app showcasing the LayoutBuilder widget to build adaptive layouts: a single-column list for mobile and a multi-column grid-like layout for desktop."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
